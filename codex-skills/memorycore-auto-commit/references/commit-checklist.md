# Commit Checklist

Before committing:

1. `git status --short`
2. `git diff --stat`
3. Review relevant `git diff`
4. Run relevant validation when feasible
5. Stage only intended files
6. Commit with a concise conventional message

Never stage unrelated user changes without explicit approval.
